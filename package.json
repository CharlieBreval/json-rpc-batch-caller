{
  "name": "json-rpc-batch-caller",
  "version": "1.0.0",
  "description": "batch call json-rpc",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.1",
    "fs": "0.0.1-security"
  }
}
